**1. 蓝方建图**

在蓝方启动区静止放好小车，启动：

```bash
cd ~/robocon11-2-vision
./run_mapping.sh
```

输入地图后缀，例如：

```text
official_0702
```

实际保存在了：

```text
~/robocon11-2-vision/maps/official_map_official_0702
```

静止约 5 秒后开车走遍场地，尽量覆盖两侧、启动区、重试区和稳定结构，最后回到建图起点附近形成闭环。

走完后，在另一个终端保存：

```bash
cd ~/robocon11-2-vision
ros2 service call /r2/sam/save_map std_srvs/srv/Trigger "{}"
```

必须看到 `success=True`，然后检查：

```bash
wc -l maps/official_map_official_0702/poses.csv
find maps/official_map_official_0702/keyframes -name '*.pcd' | wc -l
```

确认有关键帧后，再回建图终端按 `Ctrl+C`。

**2. 重定位寻点**
把小车放在蓝方启动区：

```bash
cd ~/robocon11-2-vision
./run_debug.sh
```

选择：

```text
[2] 预制地图重定位
```

再选择刚保存的地图。

另开终端确认定位真实成功：

```bash
ros2 topic echo --once --qos-durability transient_local \
  /r2/localized std_msgs/msg/Bool

ros2 topic echo --once --qos-durability transient_local \
  /r2/fitness_score std_msgs/msg/Float64

ros2 topic hz /r2/global_odometry
```

必须满足：

```text
/r2/localized: true
/r2/global_odometry 持续输出
fitness_score 越低越好，当前接受阈值为 1.0
```

**3. 记录坐标**

依次将小车停在六个点，每次完全静止后，在 `run_debug.sh` 终端输入：

```text
q
```

等待约 5 秒，记录：

```text
静止平均完成 [q 手动测量]: x=... mm y=... mm yaw=... deg
```

`q` 的平均结果只用于检查稳定性或记录路径点，`r2_pose_reporter` 不再保存启动锚点，也不会用锚点平移纯里程计坐标。

Localization 模式仍需更新 `run_competition.sh` 中对应半场的定位先验 `PRIOR_X/PRIOR_Y`；它来自 `/r2/global_odometry`，不是 `simple_odom` 的二维车体外参换算坐标。

**4. 定位失败时录包**

先停止失败的 `run_debug.sh`。终端 A 先开始录制：

```bash
cd ~/robocon11-2-vision

STAMP=$(date +%Y%m%d_%H%M%S)
ros2 bag record -o "bag_files/relocal_fail_${STAMP}" \
  /livox/lidar /livox/imu \
  /Odometry /cloud_registered /cloud_registered_body \
  /r2/global_odometry /r2/localized /r2/fitness_score \
  /r2/localization/src /r2/localization/dst \
  /r2/localization/coarse_aligned /r2/localization/final_aligned \
  /tf /tf_static
```

终端 B 重新执行：

```bash
cd ~/robocon11-2-vision
./run_debug.sh
```

保持原地失败状态至少 20 秒，然后在录包终端按 `Ctrl+C`。同时保留对应地图：

```bash
tar -czf "bag_files/relocal_fail_${STAMP}_map.tar.gz" \
  -C maps official_map_official_0702
```

记录好以下信息：bag 目录、地图压缩包、定位终端完整日志。

**5. 正式比赛**

```bash
cd ~/robocon11-2-vision
./run_competition.sh
```

依次选择红/蓝方、挑战赛/正赛、`Localization`、地图编号，最后输入：

```text
YES
```

启动后检查 `/r2/localized: true`，并在主终端输入 `q` 核对当前坐标。